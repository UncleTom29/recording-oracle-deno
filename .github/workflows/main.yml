name: CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: 1.4.2

      - name: Install dependencies
        continue-on-error: true
        run: deno cache src/server.ts src/storage.ts

      - name: Test
        continue-on-error: true
        run: deno test --allow-net

      - name: Build
        continue-on-error: true
        run: deno build src/server.ts src/storage.ts
        
      - name: Deploy to Vercel Action
        uses: BetaHuhn/deploy-to-vercel-action@v1
        with:
          GITHUB_TOKEN: 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
          VERCEL_TOKEN: W3oLUhxlcxlvkQAGB83Ao6uS
          VERCEL_ORG_ID: TvUd5JRLSOKGs7nK2GUAA7U
          VERCEL_PROJECT_ID: prj_D5YMzxLejda0FyO3mUSKNk8PgP9K
